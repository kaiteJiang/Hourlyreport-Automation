# 商务通本地 API 测试夹具

自动测试只使用合成日志、假 HTTP 响应和假数据库桥输出。

不得把以下真实内容提交到此目录：

- `clientToken`、Cookie、Authorization 请求头；
- 访客姓名、手机号、微信号或聊天正文；
- 商务通 SQLCipher 数据库；
- 人工导出的真实会话文件。

2026-07-26 的 26 条验收只在本机以只读方式执行，仓库仅记录聚合结果。
